
import React from "react";
import { CardHeader, CardTitle } from "@/components/ui/card";

const ContentGeneratorHeader: React.FC = () => {
  return (
    <CardHeader>
      <CardTitle>Générer du contenu</CardTitle>
    </CardHeader>
  );
};

export default ContentGeneratorHeader;
